defmodule Pipeline.Interpreter.Printer do
  @moduledoc """
  Print out a pipeline.
  """

  def print(pipeline, prefix \\ "") do
    # run(pipeline, [])
    # |> Enum.map(fn x -> prefix <> "~> " <> x end)
    # |> Enum.join("\n")
  end

end
