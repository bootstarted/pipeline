defmodule Pipeline.Match do

  def init(plug) do

  end

  def call(conn, opts) do
    # conn = plug.call(conn, plug_opts)
    # if (conn.private.Match) do
    #   opts.consequent.call(conn, consequent_opts)
    # else
    #   opts.alternate.call(conn, alternate_opts)
    # end
    conn
  end


  def inspect(options) do

  end



  def match(predicate) do

  end

  def host(conn, options) do

  end

  def path(path) do

  end


end
